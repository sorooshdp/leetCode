/**
 * @param {number} n
 * @param {number} index
 * @param {number} maxSum
 * @return {number}
 */
const maxValue = function (n, index, maxSum) {
    const nums = [];
    let sum = 0;

    for (let i = 0; i < n; i++) {
        nuns[i] = 0;
    }

    console.log(nums[0]);
    nums[index] = maxSum;

    while (true) {
        if (index + 1 !== undefined){
             
        }
    }

};

console.log(maxValue(4, 2, 6));