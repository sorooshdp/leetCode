function duplicateZeros(arr: number[]): void {
    const index = [];
    for (let i = 0; i < arr.length - 1; i++) {
        if (arr[i] === 0) {
            index.push(i);
        }
    }

    while ( index.length) {
        let i = arr.length - 2;
        
    }
}
